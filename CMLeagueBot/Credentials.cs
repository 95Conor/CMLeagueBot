using System;
using System.Collections.Generic;
using System.Text;

namespace CMLeagueBot
{
    public static class Credentials
    {
        public const string Token = "redacted";

        // Go into settings > appearance > enable developer mode
        // Then right click channel, click 'copy ID'.
        public const ulong ChannelID = 1;
    }
}
